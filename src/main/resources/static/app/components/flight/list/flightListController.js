/*
angular.module('app')
    .controller('FlightListController', function(FlightService) {
        const vm = this;
        vm.flight = FlightService.getAll();

        vm.search = lastName => {
            vm.flight = FlightService.getAll({lastName});
        };
    });*/
