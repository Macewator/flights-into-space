/*
angular.module('app')
    .controller('FlightTouristsController', function($routeParams, FlightService) {
        const vm = this;
        const flightId = $routeParams.flightId;
        vm.flight = FlightService.get(flightId);
        vm.tourists = FlightService.getTourists(flightId);
    });*/
